import { h, render } from 'preact';
import { useEffect, useState } from 'preact/hooks';

interface SuggestProps {

}

export function Suggest(SuggestProps: SuggestProps) {
  return (
    <div className='suggest'>suggest</div>
  );
}
